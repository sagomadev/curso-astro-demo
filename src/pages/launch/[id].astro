---
import Layout from "../../layouts/Layout.astro";
import { getLaunchById } from "../../services/spacex";

const { id } = Astro.params;

let launch;

if (id) {
  launch = await getLaunchById({ id });
}

// export const prerender = false;
---

<Layout title="Launch">prueba</Layout>
